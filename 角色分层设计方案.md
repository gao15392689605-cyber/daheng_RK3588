# 烟草异物检测系统 — 角色分层设计方案

> 版本:v1 草案 · 2026-06-04
> 适用:`tobacco_detection_system`(RK3588 单机离线 PySide6 系统)
> 目的:把当前"所有人平权"的界面,改造为**管理员 / 技术人员 / 工人**三层业务化操作,
> 实现"操作—调优—管理"职责分离、生产配置防误改、操作可追溯。

---

## 一、背景与问题

当前系统**完全平权**:任何登录用户都能切换模型、拖动 conf/iou 阈值、调相机曝光增益、甚至自助注册新账号;检测记录虽按用户存储,但**没有角色概念**。

这在工业现场有三个硬伤:

1. **质量失控**:一线工人无意拖动 conf 到 0.9,会导致大量漏检 —— 金属、石块混入烟丝是安全事故。
2. **无法追溯**:出了批次问题,查不到"漏检时用的哪套阈值、谁改的、谁当班"。
3. **账号失管**:谁都能注册账号,不符合工厂"按岗位开户"的管理要求。

老板提出的"业务化分层",本质就是要解决以上三点 —— **不是简单加三个登录,而是把三种职责和权限分开,并让生产配置稳定、可审计。**

---

## 二、三个角色定义

| 角色 | 现场对应 | 核心职责 | 一句话 |
|---|---|---|---|
| **工人(Worker)** | 产线操作员、倒班岗 | 启停检测、盯画面、处理报警 | **只管跑和处理,不碰参数** |
| **技术人员(Technician)** | 质量 / 设备工程师 | 调模型与阈值、定标相机、发布生产配置、看趋势 | **调优并锁定一套给生产用** |
| **管理员(Admin)** | 车间主管 / IT | 账号与角色管理、审计、报表导出、系统配置 | **管人、管账、管追溯** |

---

## 三、权限矩阵

| 功能 | 工人 | 技术人员 | 管理员 |
|---|:---:|:---:|:---:|
| 看实时检测 + 报警 | ✅ | ✅ | △ 只读监看(可选) |
| 开始 / 暂停 / 停止检测 | ✅ | ✅ | ❌ |
| 处理 / 确认报警(闭环记录) | ✅ | ✅ | ❌ |
| 拍摄 / 抓帧 | ✅ | ✅ | ❌ |
| 模型选择(.rknn/.pt) | ❌ | ✅ | ❌ |
| conf / iou / 预设档调节 | ❌(只读锁定值) | ✅ | ❌ |
| 相机曝光 / 增益设置 | ❌ | ✅ | ❌ |
| **发布 / 锁定生产配置** | ❌ | ✅ | ❌ |
| 检测历史 | 只看本人/本班 | 全线 + 误检分析 | 全部 + 审计 |
| 统计报表(日报/趋势) | ❌ | ✅(质量视角) | ✅(管理视角) |
| 用户增删 / 赋角色 / 改密 | ❌ | ❌ | ✅ |
| 导出数据 / 截图 | ❌ | 受限 | ✅ |
| 审计日志查看 | ❌ | ❌ | ✅ |

> 原则:**三个角色职责单一不交叉** —— 工人只操作执行、技术员唯一调优(选模型/调参/发布配置)、管理员只管账号+审计+报表,**不碰检测参数**。
> 即:管理员页 = 纯管理面板(无检测画面/无参数滑块),他登录看到的是账号管理 + 审计 + 报表,而不是检测界面。

---

## 三·补、各功能是做什么的 / 有什么作用

> 让老板和非技术同事也能看懂每个功能的业务价值,以及为什么要按角色分。

### A. 检测相关(产线核心)

> **一句话:摄像头 = 产线实时主力(工人用);照片/视频/文件夹 = 技术员的离线工具**(调参验证、回放排查、批量评估、给老板演示),工人用不到、对工人隐藏。

| 功能 | 做什么 | 作用 / 为什么要它 | 归属角色 |
|---|---|---|---|
| **摄像头实时检测** | 工业相机实时取流,逐帧检测异物 | **产线在线主用** —— 烟丝经过时实时发现异物 | 工人 / 技术 |
| **照片检测** | 选单张图片检测 | 抽检、复核某张可疑图 | 技术 |
| **视频检测** | 对视频逐帧检测 | 回放录像、事后排查某段 | 技术 |
| **文件夹批量检测** | 批量跑一堆图/视频 | 批量质检、历史数据回溯分析 | 技术 |
| **开始 / 暂停 / 停止** | 控制检测流程 | 启停产线检测、暂停看某帧 | 工人 / 技术 |
| **拍摄 / 抓帧** | 抓当前帧做检测或存原图 | 留证、把可疑帧存下来复核 | 工人 / 技术 |

### B. 参数与模型(影响检测质量 —— 高危,锁给技术)

| 功能 | 做什么 | 作用 / 为什么要它 | 归属角色 |
|---|---|---|---|
| **模型选择** | 切换 OBB / Seg、不同精度(INT8/FP16)模型 | 换检测方案或任务形态;选错精度会影响准确率 | 技术 / 管理员 |
| **conf 置信度阈值** | 多高分才算"检到" | **调"漏检 vs 误检"的平衡** —— 调低少漏检但多误检,调高反之。**工人乱调=安全风险** | 技术 |
| **iou 阈值** | NMS 去重的重叠门槛 | 抑制同一异物的重复框 | 技术 |
| **预设档(标准/高召回/高准确率/最佳)** | 一键套用调好的阈值组合 | 快速切检测倾向:高召回=不漏检场景,高准确=不误报场景;Seg"最佳"=每类最优阈值 | 技术 |
| **相机曝光 / 增益** | 调画面亮暗 | 保证输入图像质量(过曝/欠曝/噪点都会让检测变差) | 技术 |

### C. 记录与追溯

| 功能 | 做什么 | 作用 / 为什么要它 | 归属角色 |
|---|---|---|---|
| **检测结果表** | 列出本次检测到的每个目标(类别/置信度/位置) | 实时查看检测详情 | 全部 |
| **结果保存 / 导出** | 导出明细CSV / 汇总CSV / 带框图 | 留档、质量追溯、向上汇报 | 技术 / 管理员 |
| **检测历史** | 查过往检测记录 | 回溯"某时段检了什么、有没有异物" | 工人(本班)/ 技术 / 管理员 |
| **报警处理(新增)** | 异物报警→工人确认并记录处置(拣出/停线/放行) | **形成闭环**,未处理报警不丢失,可查"谁处理了什么" | 工人 |
| **生产配置发布/锁定(新增)** | 把调好的一套参数打包命名锁定 | **防工人误改**,生产用经认证的固定配置 | 技术 |
| **审计日志(新增)** | 记录谁何时改了什么参数/模型/处理了什么报警 | **批次出问题能倒查责任与配置** | 管理员 |
| **统计报表(新增)** | 日报/周报、异物趋势、班次对比、误检率 | 给管理层看的经营/质量视角 | 技术 / 管理员 |

### D. 账号与系统

| 功能 | 做什么 | 作用 / 为什么要它 | 归属角色 |
|---|---|---|---|
| **登录(验证码+密码)** | 身份认证 | 防无关人员操作产线系统 | 全部 |
| **个人中心(改资料/改密/头像)** | 自助维护账号 | 改自己的密码和信息 | 全部 |
| **账号管理(开户/赋角色/停用/重置密码)** | 管理员建账号并指定角色 | **按岗位开户**,取代现在的自助注册 | 管理员 |
| **班次/交接班(新增)** | 绑定当班人,生成交接班汇总 | 检测记录绑到人和班次,可追责 | 工人 / 管理员 |

---

## 四、各角色操作页面设计

### 4.0 登录页 —— 统一入口,角色由账号决定(方案A)

**原则:角色绑在账号上(管理员开户时指定),登录时不让用户自选角色** —— 否则谁都能选"管理员"进去。
登录页保持简洁,只去掉"自助注册"。

```
┌─────────────────────────────────┐
│      烟草异物智能检测系统          │
│   RK3588 NPU · 10 类异物检测      │
│                                 │
│   用户名  [____________]         │
│   密码    [____________]         │
│   验证码  [______]  [A7K9]🔄     │
│                                 │
│         [    登 录    ]          │
│                                 │
│   账号由管理员开通 · 忘记密码请联系管理员 │
└─────────────────────────────────┘
```

**登录流程(角色后台判,不在页面选):**
```
输账号密码 + 验证码 → 校验
   ├─ 失败 → 提示
   ├─ 账号被停用 → "账号已停用,请联系管理员"
   └─ 成功 → 读该账号 role
        ├─ 首次登录(must_change_pwd)→ 强制改密 → 再进
        ├─ role=worker     → 工人极简页(4.1)
        ├─ role=technician → 技术全功能页(4.2)
        └─ role=admin      → 管理面板(4.3)
```

**相比现在的改动:** ① 去掉"注册账号"按钮(改"账号由管理员开通");② 登录后按 role 进不同页;
③ 首登强制改密(淘汰默认弱口令);④ 验证码保留(防暴力);⑤ 登录后顶部显示"角色 + 姓名 + 班次"。

### 4.1 工人页面 —— 极简、傻瓜化(重点)

工人不需要看到 conf 滑块、模型路径、ONNX/RKNN 术语。他要的就是:**画面、报警、一个处理按钮、当班计数。**

```
┌────────────────────────────────────────────────────┐
│  当班: 张三  班次: 早班   配置: 生产V3(锁定)   09:14 │
├──────────────────────────────────┬─────────────────┤
│                                  │  ⚠ 报警           │
│                                  │  ┌─────────────┐ │
│          实时检测大画面            │  │ 金属碎片 ×1  │ │
│        (异物正框 + mask)          │  │ [ 已处理 ]   │ │
│                                  │  └─────────────┘ │
│                                  │                 │
│                                  │  当班累计:        │
│   ▶ 开始       ■ 停止             │  发丝 12 金属 1   │
└──────────────────────────────────┴─────────────────┘
```

- 只有 ▶开始 / ■停止 / 报警处理;**无参数、无模型、无相机设置入口**
- 检测到异物 → 醒目报警(声/色)→ 工人点"已处理"并选处置(拣出/停线/放行)→ 入库
- 顶部显示当前**锁定的生产配置名**(让工人知道用的是经认证的配置)

### 4.2 技术人员页面 —— 当前全功能界面 + 发布

基本就是现在这套界面(conf/iou 滑块、模型选择、预设按钮、相机设置、四种检测模式),再加:

- **"发布为生产配置"按钮**:把当前 模型 + conf + iou + 预设 + 相机参数 打包成一个命名配置(如"生产V3"),存库并标记为当前生产配置。工人端只能用,不能改。
- **误检分析**:看全线检测趋势、各类异物频次、疑似误检复核。

### 4.3 管理员页面 —— 管理面板

四块功能:**账号管理 / 审计日志 / 报表导出 / 系统配置**。其中**账号管理是创建账号的唯一入口**(A 方案去掉了自助注册,账号只能管理员开)。

**① 账号管理(核心):**
```
┌──────────────────────────────────────────────────────┐
│  账号管理                              [ + 新建账号 ]   │
├────┬─────────┬────────┬────────┬──────┬──────────────┤
│工号│ 用户名   │ 姓名   │ 角色   │ 状态 │ 操作          │
├────┼─────────┼────────┼────────┼──────┼──────────────┤
│001 │ zhangsan│ 张三   │ 工人   │ 启用 │ 改角色 重置密码 停用│
│002 │ litech  │ 李技术 │ 技术人员│ 启用 │ ...           │
│003 │ admin   │ 管理员 │ 管理员 │ 启用 │ ...           │
└────┴─────────┴────────┴────────┴──────┴──────────────┘
```

**点「+ 新建账号」弹表单:**
```
┌─────────────────────────┐
│  新建账号                │
│  用户名 [__________]     │
│  姓名   [__________]     │
│  工号   [__________]     │
│  角色   [工人 ▾]  ← 工人/技术人员/管理员 三选一  │
│  初始密码[__________](或"自动生成")  │
│  ☑ 首次登录强制改密       │
│      [取消]  [创建]       │
└─────────────────────────┘
```

| 账号操作 | 作用 |
|---|---|
| **新建账号** | 开户 + **指定角色**(决定登录后进哪个页) |
| 改角色 | 岗位变动调整(工人→技术) |
| 重置密码 | 工人忘密码 → 管理员重置 + 强制改密 |
| 停用 / 启用 | 离职/调岗停用,账号留着但登不进(保留历史可追溯) |
| (删除) | 一般用"停用"不用删除,保留追溯链 |

> **第一个管理员怎么来**:由 `db/init.sql` **预置一个默认管理员**(如 `admin` + 默认密码 + 强制首登改密),系统首次启动就有;之后所有账号都由他在页面里建。现在 init.sql 默认建的是无角色的 `operator/123456`,改造时换成预置 `admin`。

**② 审计日志**:谁、何时、改了哪个参数/模型/配置、处理了哪些报警
**③ 报表导出**:日报/周报、班次对比、异物趋势(给上级汇报)
**④ 系统配置**:数据保留期、配置版本回滚

---

## 五、核心机制(几个不能漏的点)

1. **生产配置锁定/发布**(防误改的关键)
   技术员调好 → 发布命名配置 → 工人端只读运行。杜绝一线随手改阈值导致漏检。

2. **审计追溯**(烟草受监管,必须有)
   新增审计表,记录:参数变更、模型切换、配置发布、登录登出、报警处置。出批次问题能倒查。

3. **视觉分级报警 + 严重项处理闭环**(当前手动处置场景,纯视觉无声)

   **报警分级**(哪些算"严重"由技术员可配,默认金属/金属碎片/螺帽/石块):

   | 级别 | 异物 | 提示方式 | 处理 |
   |---|---|---|---|
   | 🔴 严重 | 金属/铁钉/螺帽/石块(硬质) | **画面变红 + 横幅闪烁 + 框高亮**,打断式 | 必须确认 + 存证据帧 + 查上游 |
   | 🟡 一般 | 发丝/棉絮/纤维/麻绳(软质) | 角落小标 + 计数,不打断 | 拣出即可,不强制逐个确认 |

   **严重项处理链路(下游分流,主线不停):**
   ```
   ① 检测点检出铁钉/金属
   ② 下游分流闸门(按带速延时)→ 把那一份分流到隔离区   [主线不停, 不为异物停整线]
   ③ 工人页红色报警:"已自动分流隔离 ⚠ 请排查上游设备是否损坏/掉件"
   ④ 工人查上游设备 → 回填排查结果(闭环:谁查的/查到啥)
   ```
   - **检测连续不中断**:检出一个不影响后续帧继续检,系统照常往下检
   - **不为每个异物停整线**:一般项从不触发停线;严重项也只"局部分流那一份"
   - **频次升级**:短时间内连续多个金属(如 5 分钟 ≥3)→ 升级报警"上游设备可能正在掉件,建议立即停线检修" —— 抓住"机器正在坏"的窗口
   - **隔离区**:分流出的料攒在隔离区,技术员定期复检(也是查模型准不准的样本)
   - **声音/声光报警**:暂缓(RK3588 技术上支持,后期接喇叭/GPIO 声光柱);**当前纯视觉**

4. **班次 / 交接班**
   登录绑定班次,生成交接班汇总(本班检测量、报警处置、漏报),记录绑到当班人。

5. **受控开户**
   砍掉登录页自助注册,改为管理员开户并赋角色;首登强制改密(现默认 `operator/123456` 必须淘汰)。

6. **权限硬实现**
   不只隐藏 UI 按钮,后端在高危操作前加角色守卫(工人即使绕过界面也无法调用 `_choose_model` 等)。

---

## 六、改造点(技术清单,落地参考)

| 模块 | 改动 |
|---|---|
| `db/init.sql` | `users` 表加 `role`(admin/technician/worker)+ `must_change_pwd`;新增 `audit_logs` 表、`prod_config` 表(配置版本)、班次字段 |
| `db/db_helper.py` | 加 角色查询、审计写入、配置发布/读取、按班次查询 |
| `core/app_state.py` | state 加 `role` / `shift` / 当前生产配置 |
| `ui/login_window.py` | 去掉自助注册;登录后按 `role` 分发到不同主页;首登强制改密 |
| `ui/main_window.py` | 按 role 显隐导航与控制栏;高危操作(`_choose_model`/`_on_conf_changed`/相机设置)加权限守卫;接报警闭环 |
| 新增 `ui/worker_window.py` | 工人极简页面(大画面 + 报警 + 处理) |
| 新增 `ui/admin_panel.py` | 账号管理 + 审计 + 报表 |
| `core/detector.py` / 推理回调 | 异物触发报警事件 → 按严重度分级 → 工人确认 → 写处置记录;严重类频次统计(触发升级报警) |
| 新增 严重项分流逻辑 | 严重类检出 → 按带速延时触发**下游分流闸门**(RK3588 GPIO/继电器)→ 分流到隔离区(主线不停) |
| 报警分级配置 | 严重类名单、频次升级阈值、分流延时 等参数纳入"生产配置",技术员可调 |

---

## 七、分期落地建议

- **P0(先做,解决老板痛点)**:`role` 字段 + 三角色登录分页 + 工人界面锁参数 + 受控开户。→ 立刻实现"业务化分层"的观感和质量防护。
- **P1(补管理闭环)**:审计表 + 报警闭环 + 生产配置发布/锁定。
- **P2(锦上添花)**:班次交接、管理报表/趋势、配置版本回滚。

---

## 八、合规呼应(可向上汇报的点)

本方案契合食品/烟草行业的质量管理要求:**操作权限分级、关键参数受控、操作全程可追溯、账号按岗管理** —— 既防止一线误操作导致漏检,又能在出现质量问题时快速倒查责任与配置,支撑批次追溯。
